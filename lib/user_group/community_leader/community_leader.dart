import 'package:flutter/material.dart';

class CommunityLeader extends StatelessWidget {
  const CommunityLeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
