import 'package:flutter/material.dart';

class DonationList extends StatelessWidget {
  const DonationList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("Senarai Barang Boleh Didermakan")
      ],
    );
  }
}
