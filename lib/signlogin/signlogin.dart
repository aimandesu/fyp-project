import 'package:flutter/material.dart';

class SignLogin extends StatelessWidget {
  const SignLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("sign login"));
  }
}
