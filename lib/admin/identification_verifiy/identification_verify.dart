import 'package:flutter/material.dart';

class IdentificationVerification extends StatelessWidget {
  const IdentificationVerification({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Verification");
  }
}
