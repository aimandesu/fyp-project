import 'package:flutter/material.dart';

class MapCentre extends StatelessWidget {
  const MapCentre({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
