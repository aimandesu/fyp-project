import 'package:flutter/material.dart';

class OnWatch extends StatelessWidget {
  const OnWatch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("On Watch");
  }
}
